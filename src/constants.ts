export const ADDON_ID = 'storybook/docs-html';
export const PANEL_ID = `${ADDON_ID}/panel`;
export const KEY = `storybook/docs-html`;
export const PARAM_KEY = `docsHtml`;
export const COPIED_TIMEOUT = 2000;

export const EVENTS = {
  CODE_UPDATE: `${ADDON_ID}/code-update`,
  RESULT: `${ADDON_ID}/result`, // delete
  REQUEST: `${ADDON_ID}/request`, // delete
};
